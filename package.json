{
  "name": "x402-flash",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces",
    "deploy": "bash scripts/deploy-program.sh"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "eventemitter3": "^5.0.1",
    "ws": "^8.18.3"
  }
}
